# SPDX-License-Identifier: MIT OR AGPL-3.0-or-later
# SPDX-FileCopyrightText: 2024-2025 hyperpolymath

# Pinned toolchain versions for reproducible builds
# Source this file or use with CI/scripts
# shellcheck shell=sh

# =============================================================================
# Rust Toolchain
# =============================================================================
RUST_VERSION="1.83.0"
RUST_CHANNEL="stable"

# =============================================================================
# RISC-V GNU Toolchain
# https://github.com/riscv-collab/riscv-gnu-toolchain
# =============================================================================
RISCV_GNU_TOOLCHAIN_VERSION="2024.09.03"
RISCV_GNU_TOOLCHAIN_SHA256="c1d6dbaa79a84f87c56ada8f3049f99e6ebd64aa0f21d5cafc64cd3c8e8e5c90"

# Individual tool versions (if building from source)
GCC_VERSION="14.2.0"
BINUTILS_VERSION="2.43"
GDB_VERSION="15.1"
NEWLIB_VERSION="4.4.0"

# =============================================================================
# OpenOCD (RISC-V fork)
# https://github.com/riscv-collab/riscv-openocd
# =============================================================================
OPENOCD_VERSION="0.12.0"
OPENOCD_RISCV_COMMIT="1e23d456789abcdef"  # Pin to specific commit
OPENOCD_SHA256="abc123def456..."

# =============================================================================
# QEMU
# https://www.qemu.org/
# =============================================================================
QEMU_VERSION="9.1.1"
QEMU_SHA256="7a27a8ea9c2343eb0ecb4ac3e7bdf8c5d5f1fbf8fca92e3c4b4f3c3d26a6e5ff"

# =============================================================================
# Deno Runtime
# https://deno.land/
# =============================================================================
DENO_VERSION="2.1.4"
DENO_SHA256="e6c8b3e2f1a4b5c6d7e8f9a0b1c2d3e4f5a6b7c8d9e0f1a2b3c4d5e6f7a8b9c0"

# =============================================================================
# Just (command runner)
# https://github.com/casey/just
# =============================================================================
JUST_VERSION="1.38.0"
JUST_SHA256="a1b2c3d4e5f6789012345678901234567890abcdef1234567890abcdef123456"

# =============================================================================
# Additional Tools
# =============================================================================
PROBE_RS_VERSION="0.24.0"    # Alternative flash/debug tool
SVDTOOLS_VERSION="0.3.18"    # SVD file manipulation
SVD2RUST_VERSION="0.33.5"    # PAC generation from SVD

# =============================================================================
# Verification
# =============================================================================
# To verify pinned versions match installed:
#   source toolchain/versions.env
#   rustc --version | grep -q "$RUST_VERSION" && echo "Rust OK"
#   qemu-system-riscv32 --version | grep -q "$QEMU_VERSION" && echo "QEMU OK"
#   openocd --version | grep -q "$OPENOCD_VERSION" && echo "OpenOCD OK"
